- [강제지침] 한글로 얘기해줘
- [강제지침] commit description 은 한글로 작성
- [강제지침] 변경은 최소한으로 해주고 하드코딩, 매직넘버 같은 안티패턴으로 구현하지 말아줘
- [강제지침] 스켈레톤 (trait 또는 interface 같은) 부터 작성, 스켈레톤끼리 조합해서 구조를 잡은 후 내부 구현하는 방식으로 처리해줘
- [강제지침] subagent 위임 시에는 작업 성격에 맞는 기본 `max_turns` 값의 2배를 지정하되, `(turn limit)`으로 끝난 작업은 결과가 미완인지 판단하고, 미완이면 `resume`으로 이어서 완료할 것.
- [강제지침] read/bash/edit/write/subagent 등 어떤 도구 호출 전에도 반드시 `판단: 직접/위임 — 이유: ...` 한 줄을 먼저 출력한다. 이 줄 없이는 도구를 호출하지 않는다.
- [강제지침] 도구 호출 전 가능하면 `범위: ...`, `주의: ...`도 함께 짧게 출력해 수정 대상과 비대상/검증/커밋 주의사항을 명시한다.
- [강제지침] 위임 후 직접 수정으로 전환하거나 직접 처리에서 위임으로 전환할 때도 다시 `판단:` 줄을 출력한다.
- [강제지침] 서브에이전트 위임 기준: 다음 상황에서는 직접 처리하지 말고 용도에 맞는 빌트인 서브에이전트에 위임한다(8개).
  - [강제지침] scout: 코드 탐색/정찰이 여러 파일에 걸치고 결과는 압축 요약만 필요할 때
  - [강제지침] researcher: 웹 리서치·외부 자료 조사 후 집중된 리서치 브리프가 필요할 때
  - [강제지침] context-builder: 요구사항·코드베이스를 분석해 작업 컨텍스트/메타프롬프트를 설계해야 할 때
  - [강제지침] planner: 컨텍스트·요구사항으로 구현 계획을 세워야 하는 다단계 작업일 때
  - [강제지침] oracle: 설계 일관성 검증·의사결정 표류 방지·중요 핸드오프 승인이 필요할 때
  - [강제지침] reviewer: 변경분·PR·계획·코드베이스 건강성 리뷰
  - [강제지침] worker: 일반 구현 작업, 수 분 이상 걸리는 빌드/추출/테스트(async), 서로 의존 없는 독립 작업 2개 이상(병렬)
  - [강제지침] delegate: 부모 모델을 그대로 쓰는 경량 위임이 필요할 때(기본 read 없음)
  - [강제지침] 예외(직접 처리): 파일 1~2개 단발 수정, grep/read 몇 번으로 끝나는 확인
  - [강제지침] 위 예외 이외에 직접 처리 해야하는 경우는 worker 에게 위임할수 있을 때까지 정보를 수집해서 `context: fork` worker로 위임한다(부모 컨텍스트 상속 → 일관성 유지 + 메인 컨텍스트 절약)
  - [강제지침] 위임/직접 처리 전 한 줄로 "왜 위임/직접인지" 판단 근거를 남긴다.
- [강제지침] 주석은 장황하지 않게 핵심만 간단히 작성해줘
