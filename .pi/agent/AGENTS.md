- [강제지침] 한글 답변. commit description도 한글.
- [강제지침] 변경 최소화, 하드코딩/매직넘버 금지, 주석은 핵심만 짧게.
- [강제지침] 구현 순서: trait/interface 등 스켈레톤 → 조합 → 내부 구현.
- [강제지침] ./.pi 커밋 금지.
- [강제지침] 모든 도구 호출 전 `판단: 직접/위임 — 이유: ...` 출력. 가능하면 `범위: ...`, `주의: ...`도 짧게. 직접↔위임 전환 시 재출력.
- [강제지침] 직접 처리 금지. 작업은 builtin subagent에 위임: scout=탐색/단순확인, researcher=웹조사, context-builder=컨텍스트화, planner=계획, oracle=설계검증, reviewer=리뷰, worker=구현/빌드/테스트/병렬, delegate=경량위임.
- [강제지침] 단순 확인=scout, 소규모 수정=worker 위임.
- [강제지침] subagent는 기본 `max_turns`의 2배. `(turn limit)`이면 미완 판단 후 필요 시 `resume`.
- [강제지침] async/background subagent에 `timeoutMs`/`maxRuntimeMs` 금지. 대신 `control.needsAttentionAfterMs`; timeout 필요 시 foreground.
